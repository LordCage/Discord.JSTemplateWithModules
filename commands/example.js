// Name this file based on the command (if the file is named test.js it will execute [prefix + test])
exports.run = (client, message, args) => {
    
};

exports.help = {
    name: 'example',
    description: '',
    usage: ''
}